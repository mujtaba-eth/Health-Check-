# Optional: enables adaptive AI questions and report generation.
OPENAI_API_KEY=your_openai_api_key_here
# Optional override. The default is gpt-4o-mini.
OPENAI_MODEL=gpt-4o-mini
# Optional: only needed when the frontend and API are deployed separately.
VITE_API_BASE_URL=
